# RC-car wrong-direction braking
# In bidir+rc_car mode, applying throttle in wrong direction should
# set adjusted_input=0, engage prop brake, check return_to_center.

@config
armed=1
inputSet=1
dshot=1
EDT_ARMED=1
running=1
old_routine=0
zero_crosses=200
commutation_interval=1000
eeprom.bi_direction=1
eeprom.rc_car_reverse=1
eeprom.comp_pwm=1

@sequence
# Forward direction with positive throttle
ticks 5 | throttle=1500 | running=1

# Reverse direction — should trigger wrong-direction braking
# (newinput > 1048 = reverse in bidir, forward motor → wrong direction)
ticks 5 | throttle=500 | prop_brake_active=1
