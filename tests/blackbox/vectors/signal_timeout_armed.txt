# Signal timeout — armed ESC disarms after 0.5s signal loss
# REQ-SAFE-SIGNAL_TIMEOUT
# Golden values captured from C harness.
#
# C firmware: armed + signaltimeout > 10000 → allOff, disarm, system reset
# Rust: armed + signaltimeout > 10000 → Disarm transition only
# GAP: Rust missing allOff (FET-off), DMA reset, system reset

@config
armed=1
inputSet=1
dshot=1
EDT_ARMED=1
running=1
old_routine=1

@sequence
# Motor running, then signal lost (throttle=-1 disables injection)
tick 1  | throttle=500 | armed=1
config throttle=-1
ticks 12000 | | armed=0
