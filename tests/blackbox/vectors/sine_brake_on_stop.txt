# Sine mode brake on stop
# When stepper_sine=1 and throttle=0 (or !armed), brake modes engage.
# brake_on_stop=1 → proportional brake, =2 → comStep(2), =0 → allOff.

@config
armed=0
inputSet=1
dshot=1
stepper_sine=1
eeprom.use_sine_start=1
eeprom.comp_pwm=1
eeprom.brake_on_stop=1
eeprom.drag_brake_strength=5

@sequence
# Unarmed in sine mode — brake_on_stop=1 → proportional brake
ticks 3 | throttle=0 | prop_brake_active=1 pwm_duty>0
