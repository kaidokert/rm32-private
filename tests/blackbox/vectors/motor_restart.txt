# Motor restart when !old_routine
# When old_routine=0 and running=0, throttle >= 47 should call startMotor().

@config
armed=1
inputSet=1
dshot=1
EDT_ARMED=1
running=0
old_routine=0
zero_crosses=0
commutation_interval=0

@sequence
# Throttle above threshold with motor stopped should restart
# allOff called before startMotor to clear phase outputs
tick 1  | throttle=500 | running=1 alloff_count>0
