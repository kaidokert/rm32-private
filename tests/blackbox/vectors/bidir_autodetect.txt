# Bidirectional DShot auto-detection
# When input pin is HIGH for 100+ consecutive DShot frames while unarmed,
# dshot_telemetry should be set to 1 (inverts CRC for bidir mode).

@config
armed=0
inputSet=1
dshot=1
zero_input_count=31

@sequence
# Set input pin high (bidir idle state)
config input_pin_state=1

# Send 110 DShot frames with pin high — should trigger bidir detection
# dshot_frame=0 is zero throttle, triggers transfercomplete each tick
ticks 110 | dshot_frame=0 | newinput=0
