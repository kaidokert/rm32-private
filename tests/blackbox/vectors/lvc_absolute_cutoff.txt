# LVC mode 2 — absolute voltage cutoff
# REQ-PROT-LVC (mode 2): compares battery_voltage directly against
# eeprom.absolute_voltage_cutoff (no per-cell scaling).
#
# XFAIL on C harness: ADC mock overwrites battery_voltage every tick
# so manually-set low voltage doesn't persist through the LVC check.
# The mode 2 path IS exercised (low_voltage_cut_off==2 branch entered)
# but the voltage comparison doesn't trigger because ADC resets it.

@config
armed=1
inputSet=1
dshot=1
EDT_ARMED=1
eeprom.low_voltage_cut_off=2
eeprom.absolute_voltage_cutoff=100
running=1
old_routine=1

@sequence
# Set battery voltage below absolute threshold
config battery_voltage=50
config low_voltage_count=9999

# Verify mode 2 branch is entered (even though ADC overwrites voltage)
# Just confirm the system doesn't crash with mode 2 config
ticks 30 | throttle=500 | running=1
